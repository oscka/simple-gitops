apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simple-api-ingress-blue
  namespace: deployment-strategy
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: "simple-api-blue.192.168.0.58.sslip.io"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: simple-api-svc
            port:
              number: 8080
