apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simple-api-ingress-active
  namespace: deployment-strategy
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: "simple-api-active.192.168.0.58.sslip.io"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: simple-api-active
            port:
              number: 8080
