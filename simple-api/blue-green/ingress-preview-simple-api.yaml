apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simple-api-ingress-preview
  namespace: deployment-strategy
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: "simple-api-preview.192.168.0.58.sslip.io"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: simple-api-preview
            port:
              number: 8080
