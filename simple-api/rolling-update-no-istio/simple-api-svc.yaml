apiVersion: v1
kind: Service

metadata:
  name: simple-api-svc
  namespace: sample
spec:
  selector:
    app: simple-api-test
  ports:
  - protocol: TCP
    name: http
    port: 8080
    targetPort: http